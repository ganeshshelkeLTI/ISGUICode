import { NoDecimalDirective } from './no-decimal.directive';

describe('NoDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDecimalDirective();
    expect(directive).toBeTruthy();
  });
});

import { NegativeNumberDirective } from './negative-number.directive';

describe('NegativeNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NegativeNumberDirective();
    expect(directive).toBeTruthy();
  });
});

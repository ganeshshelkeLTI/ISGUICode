import { AuthorizerDirective } from './authorizer.directive';

describe('AuthorizerDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizerDirective();
    expect(directive).toBeTruthy();
  });
});
